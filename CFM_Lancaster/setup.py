from setuptools import find_packages, setup

setup(
    name="src",
    packages=find_packages(),
)

# setup(
#     # ...
#     package_dir = {"": "src"}
#     # directory containing all the packages (e.g.  src/mypkg, src/mypkg/subpkg1, ...)
# )

# #python print folder of current 
